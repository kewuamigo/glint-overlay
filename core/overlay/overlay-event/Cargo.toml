[package]
name = "glint-overlay-event"
description = "Glint events"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true

[features]
bincode = ["dep:bincode"]

[dependencies]
bincode = { version = "2.0.1", optional = true, features = ["derive"] }
bitflags = "2.9.1"
