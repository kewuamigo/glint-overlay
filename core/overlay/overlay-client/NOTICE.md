# Third-party origin: overlay injection/compositor

Vendored and rebranded from upstream overlay compositor code (MIT OR Apache-2.0).
Integrated in-tree under `core/overlay/overlay-*` as `glint-overlay-*` crates.
